import { onLoad } from '../{{entity}}Form';
import { XrmMockGenerator } from 'xrm-mock';

describe('on Lood', () => {
  beforeAll(() => {
    XrmMockGenerator.initialise();
  });

  it('runs on load', () => {
  });
});
