{
  "version": "1.0.0",
  "name": "{{kebabCase name}}",
  "private": true,
  "scripts": {
    "gen": "plop",
    "auth": "dataverse-deploy auth",
    "deploy": "npm run auth && dataverse-deploy assembly"
  }
}
